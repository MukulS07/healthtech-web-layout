# CLAUDE.md

Project memory for this repo. Read this before doing any work here — it captures every decision
made with the user so far, so you don't have to re-ask.

## What this project is

A surgery / specialist-care booking website. Same business domain as **pristyncare.com** (used
only as a structural/design reference — see "IP boundary" below), built with the user's **own
brand, content, and database**. The user explicitly wants the site "exactly like" the reference
in terms of sitemap, page types, and design system, while using their own content.

- Owner: Sarang Shaw (sarang@doctar.in)
- Reference site: https://www.pristyncare.com
- Working placeholder brand name in the live code: **"Prime Care"** (not the final brand — replace
  with the real name/logo before launch; see IP boundary below).
- **Repo location note:** this file lives at `D:\surgery\CLAUDE.md`, one level above the actual git
  repository, which is `D:\surgery\healthtech-web-layout\`. A copy of this file is also committed
  inside that repo (`healthtech-web-layout/CLAUDE.md`) so it travels with the code and gets pushed;
  keep the two in sync when editing.

## History — this project changed tech stack and builder mid-flight (important context)

An earlier session in this same working directory planned a **hand-written Next.js + Express +
Prisma + PostgreSQL** stack (see the "Superseded original plan" section at the bottom of this file
for that record) and started scaffolding it directly under `D:\surgery`, citing a sandboxed
environment with no package-registry access. That scaffold was **abandoned** — the git repo that
actually exists today, `D:\surgery\healthtech-web-layout\`, is a **completely different app**,
generated by **Lovable** (lovable.dev) on a **Vite + TanStack Start + MongoDB** stack, with
GitHub as the source of truth (`origin`/`upstream` = `MukulS07/healthtech-web-layout`).
**This is now the one real, live project** — CLAUDE.md was previously out of sync with it (still
describing the old Next.js/Postgres plan) and has been rewritten below to describe what's actually
in the repo. Also: `npm install` **does** work in this environment (verified) — the earlier
"no network access" sandbox note no longer applies here.

## IP boundary — what we reuse vs. what must be original

**Reuse (from the reference site) — this is normal, not infringement:**
- Page taxonomy / sitemap shape: treatment pages, doctor profiles, city/clinic pages, blog, booking flow
- The general patient journey / UX flow
- Proven UI patterns: persistent booking form, trust badges, specialty-pill filters, FAQ accordions, testimonial cards
- Exact design tokens (colors, spacing unit, font choice, radius/shadow system) — see "Design system"
  below. Colors, spacing units, and using a given Google Font are not the kind of thing copyright
  protects, so matching them exactly is normal competitive design.

**Must be original (legally required, not just stylistic):**
- Brand name and logo (current placeholder in code: "Prime Care" — replace with the real name)
- All written copy (treatment descriptions, doctor bios, blog posts, FAQs)
- All photography/imagery (own photos or licensed stock — never the reference site's images)
- All patient testimonials/reviews — **never reuse real patient names, cities, or quotes scraped
  from the reference site's review wall.** The user uploaded a capture that included real Pristyn
  Care patient reviews (names, cities, treatments); that content must NOT be used as seed/placeholder
  data. Write structurally-similar but clearly fictional placeholder reviews instead.

## Tech stack (actual, as implemented in `healthtech-web-layout/`)

- **Framework:** Vite 8 + **TanStack Start** (`@tanstack/react-start`) with **TanStack Router**
  file-based routing (`src/routes/`) — a single full-stack app, not a separate frontend/backend.
  React 19, TypeScript, Tailwind CSS v4.
- **Data fetching:** TanStack Query on the client; server-side logic lives in **TanStack Start
  server functions** (`createServerFn`, under `src/lib/server-functions/`) called directly from
  route components/loaders — there is no separate REST/Express API layer.
- **Database:** **MongoDB via Mongoose** (not PostgreSQL/Prisma — that was the abandoned earlier
  plan). Connection helper: `src/lib/db.ts`. If `MONGODB_URI` isn't reachable (e.g. no local Mongo
  installed), it **automatically falls back to an in-memory MongoDB instance**
  (`mongodb-memory-server`) so the app still runs — but that data is lost on every dev-server
  restart. For real persistence, either run local MongoDB or point `MONGODB_URI` (in `.env`, see
  `.env.example`) at a hosted cluster (e.g. MongoDB Atlas).
- **Validation:** Zod is a dependency and used in the auth server functions
  (`src/lib/server-functions/auth.ts`); other server functions currently do lighter manual checks.
- **Auth:** custom, not a third-party auth provider — see "Authentication" section below.
- **Built/scaffolded by Lovable**, then hand-edited by Claude Code in later sessions. Branch syncs
  back to the Lovable editor — avoid force-pushing or rewriting published history on `main`
  (see `AGENTS.md` in the repo).

## Project structure (actual)

```
healthtech-web-layout/            ← the real git repo (origin: MukulS07/healthtech-web-layout)
├── CLAUDE.md                     ← copy of this file, kept in sync
├── AGENTS.md                     ← Lovable sync warning (don't force-push main)
├── .env.example                  ← MONGODB_URI template
├── src/
│   ├── routes/                   TanStack Router file-based routes (see Sitemap below)
│   ├── components/
│   │   ├── home/                 Header, Footer, Hero, Sections, ConsultForm, primitives
│   │   └── auth/                 AuthPanel (login/signup form, used on /contact and /account)
│   ├── hooks/use-current-user.ts  React Query wrapper around the session/user server functions
│   ├── models/                   Mongoose schemas: User, Session, Doctor, Hospital, Treatment,
│   │                              Consultation
│   ├── lib/
│   │   ├── db.ts                 Mongoose connection (+ in-memory fallback)
│   │   ├── auth.ts               password hashing (scrypt), session cookie issue/verify
│   │   ├── utils.ts, error-*.ts  misc helpers, SSR error page wrapper
│   │   └── server-functions/     doctors.ts, hospitals.ts, treatments.ts, consultations.ts,
│   │                              seed.ts, auth.ts  — all `createServerFn` handlers
│   ├── router.tsx, start.ts, server.ts   TanStack Start app wiring
│   └── routeTree.gen.ts          AUTO-GENERATED by the router plugin — don't hand-edit except to
│                                  fix it up if a route file is added/removed while the dev server
│                                  isn't running to regenerate it itself
└── .lovable/                     Lovable's own project metadata/plan notes (not authoritative —
                                    this CLAUDE.md is)
```

## Design system — ⚠️ mismatch between documented spec and actual code (unresolved)

The original design-capture spec (colors, type, spacing — recorded verbatim in the "Superseded
original plan" section at the bottom of this file) called for **navy `#00263E` + orange `#FF8300`**
and the **Lexend** typeface. The code that's actually live today (`src/styles.css`,
`src/routes/__root.tsx`) instead:
- Loads **Manrope + Sora** as the fonts (no Lexend), and
- Defines its color tokens (`--navy`, `--brand-blue`, `--brand-orange`, `--primary`, etc.) in
  `oklch()`, several of which currently resolve to the **same green-ish value**
  (`oklch(0.52 0.135 148)`) rather than distinct navy/orange hues — i.e. the orange accent color in
  the design spec isn't actually orange in the deployed CSS.

This was **not intentionally decided** — it's Lovable-generated scaffolding that drifted from the
original spec and hasn't been reconciled. **Flag this to the user before "fixing" it**: it's a
real product decision (keep the current green-leaning look vs. restyle to match the documented
navy/orange spec) — don't silently pick one.

## Sitemap (routes) — actual, from `src/routes/`

```
/                              Home (Hero, ConsultForm, doctor/hospital sections — static content)
/about                         About the company (static)
/account                       "My Appointments" — login/signup gate, then the logged-in patient's
                                own bookings with status (see Authentication below)
/contact                       Book an appointment — login/signup gate, then the booking form
                                (DB-backed: Consultation)
/doctors                       Doctor directory — DB-backed (Doctor), filter by city/specialty/sort
/doctors/$slug                 Doctor profile
/hospitals                     Hospital directory — DB-backed (Hospital), filter by city
/hospitals/$slug               Hospital profile
/treatments                    Treatment directory — DB-backed (Treatment), filter by category
/treatments/$slug              Treatment detail
/specialities/$slug            Specialty landing — STATIC placeholder content (no Specialty model)
/locations, /locations/$city   City directory / city page — STATIC placeholder content
/blog, /blog/$slug             Health articles — STATIC placeholder content (no BlogPost model)
/reviews                       Patient reviews wall — STATIC placeholder content (no Testimonial model)
/faqs                          General FAQs (static)
/privacy, /terms               Legal (static)
```

Removed in this project's history: **`/db-status`** — a Lovable-generated MongoDB diagnostics
dashboard that was linked from the header nav (disguised as "Hospital Availability") and exposed
**every patient's name/phone with no login required**. Deleted entirely, along with the
unauthenticated `getConsultationsFn` and the `restore.ts` archive-import server functions it
depended on. Don't recreate anything like it without authentication in front of it.

## Data model (actual — Mongoose, in `src/models/`)

- **`User`** — name, email (unique), phone, `passwordHash` (scrypt, salted).
- **`Session`** — `tokenHash` (SHA-256 of the session cookie value, raw token never stored),
  `userId`, `expiresAt` (Mongo TTL index auto-deletes expired sessions).
- **`Doctor`**, **`Hospital`**, **`Treatment`** — as seeded by `src/lib/server-functions/seed.ts`
  (auto-seeds on first read if the collection is empty).
- **`Consultation`** — booking submissions: name, phone, email, treatment, city, message,
  `status` (`pending | contacted | completed | cancelled`), and an optional `userId` linking it to
  the account that booked it.
- **Not implemented yet:** `Specialty`, `City`, `Clinic`, `BlogPost`, `Testimonial` models — the
  pages that would use them (`/specialities`, `/locations`, `/blog`, `/reviews`) currently render
  static placeholder content, not real DB queries. The Prisma schema with these models that an
  earlier session sketched was for the abandoned Next.js/Postgres plan and does not exist in this repo.

## Authentication (added; not in the original plan)

Booking now **requires an account** — this was an explicit user decision (patients must be able to
look up their appointment status later, which is impossible for anonymous submissions).

- Custom cookie-session auth, no third-party provider. Passwords hashed with Node's built-in
  `crypto.scrypt` (no new dependency). Session cookie is HTTP-only, `sameSite: lax`, 30-day
  expiry, verified against the `Session` collection (`src/lib/auth.ts`).
- **`/contact`** and the homepage **`ConsultForm`** widget (used on most detail pages) both show
  **only a login/signup panel** (`src/components/auth/AuthPanel.tsx`) until the visitor
  authenticates — the booking form fields are not rendered at all for guests.
- Once logged in, the form prefills name/phone/email from the account, and every submitted
  `Consultation` is tagged with that user's `userId`.
- **`/account`** ("My Appointments") shows the logged-in patient's own bookings and status
  (`getMyConsultationsFn`, scoped to `req.user`) — pending/contacted/completed/cancelled.
- **Not built yet:** password reset, rate-limiting on login attempts, and any way to *change* a
  booking's status — every booking shows "Pending" until someone edits the database directly
  (there's no admin UI). That's future work, not a bug.
- Server functions and models to know about: `src/lib/server-functions/auth.ts`
  (`signupFn`, `loginFn`, `logoutFn`, `getCurrentUserFn`), `src/hooks/use-current-user.ts` (client
  React Query wrapper).

## Build roadmap / status (current)

- [x] Core pages live and DB-backed where it matters: home, doctors, hospitals, treatments
      (listing + detail), contact/booking.
- [x] Static placeholder pages: about, blog, specialities, locations, reviews, faqs, privacy, terms.
- [x] Patient authentication: signup/login, session cookies, booking gated behind login,
      `/account` appointment-status page.
- [x] Removed the exposed `/db-status` diagnostics page and all patient-facing "MongoDB" wording.
- [ ] **Reconcile the design-system mismatch** (see above) — decide navy/orange+Lexend vs. the
      current green-leaning Manrope/Sora look, then apply consistently.
- [ ] Real `Specialty`/`City`/`Clinic`/`BlogPost`/`Testimonial` models + real content, swapping out
      the static placeholder pages.
- [ ] Persistent database — currently falls back to in-memory MongoDB and loses data on every
      restart unless the user sets up real MongoDB (local or Atlas) and a `.env`.
      Password reset + login rate-limiting.
- [ ] Admin view to update appointment status (currently everything stays "Pending" forever).
- [ ] SEO/structured data, sitemap.xml, deploy.
- [ ] Final real brand name/logo to replace "Prime Care" placeholder.

---

## Superseded original plan (historical record only — do not follow)

The sections below describe a **Next.js + Express + Prisma + PostgreSQL** plan from an earlier
session. It was never built as the live project — see "History" at the top of this file. Kept
here only so the earlier design-token capture and full sitemap research aren't lost; check the
"Design system" and "Sitemap" sections above for what's actually true of the current codebase.

### Original tech stack (not used)

- Frontend: Next.js 14 (App Router), TypeScript, Tailwind CSS, server components fetching a REST API.
- Backend: Node.js + Express + TypeScript, PostgreSQL via Prisma ORM, Zod validation.
- Database: PostgreSQL (explicit choice over MongoDB/MySQL at the time — superseded; the live repo
  uses MongoDB).

### Original design-token capture (from the user's capture of pristyncare.com, 2026-09-07)

**Colors:**
| Role | Hex | Notes |
|---|---|---|
| Background | `#FFFFFF` | primary canvas |
| Background secondary | `#00263E` | deep navy — cards/surfaces, alternating sections |
| Primary accent / CTA | `#FF8300` | brand color, buttons, links, highlights |
| Secondary accent | `#F97316` | hover states, complementary highlights |
| Text primary | `#000000` | headings and body |
| Text secondary | `#666666` | muted text, captions, placeholders |
| Border / surface tint | `#F5F3F3` | dividers, card fills, input borders |

Other colors seen in the extraction but used sparingly/contextually: `#3D7CC9`, `#00457D`,
`#0000EE` (default link blue — likely unintentional), `#005DA5`, `#BA1A1A` (error/destructive).
Form-state variables: info `#0071BC`, success `#00B079`, error `#DC332E`.

**Typography:** single typeface **Lexend** (Google Font). H1: 50px/550/52.5px line-height. H2:
60px desktop (650 weight), 30px token for smaller contexts. H3: 20–22px/700. H4: 16–20px/600.
Body: 15–16px/400. Small/caption: 12–14px/400.

**Spacing:** base unit 10px; all gaps/padding multiples of 10.

**Radius:** buttons 8–10px; inputs 2–5px; cards up to 24px, occasionally pill-shaped (50px).

**Shadows:** chromatic/tinted, not pure black — e.g. `rgba(0, 27, 63, 0.06) 0px 6px 22px 0px`,
`rgba(0, 0, 0, 0.1) 0px 0px 24px 0px`.

**Component notes:** primary CTAs often ghost/text-style; outline button variant (transparent bg,
0.8px solid border, 5px radius, bold 700 text). Cards: `#F5F3F3` bg, 24px radius, tinted shadow,
padding on inner content not the container. Don't invent UI ornaments the reference doesn't have.

### Original homepage structure research (12 sections)

Hero (3-tab search) → Patient Experiences (Pre/During/Recovery) → Hospital/clinic network →
Six-step patient journey → Doctor carousel (specialty filter + sort) → Trust wall (stats +
tagged testimonial cards) → Insurance strip → About → Book FREE Consultation → FAQ → Patient
success stories (video grid) → App download. The live homepage (`src/components/home/Sections.tsx`,
`Hero.tsx`) is loosely inspired by this but was implemented independently by Lovable — verify
against the actual component code rather than assuming this list is current.

### Original sitemap research (superseded by "Sitemap (routes) — actual" above)

```
/                              Home
/treatments                    Treatment directory (?specialty= filter)
/treatments/[slug]             Treatment detail
/specialities/[slug]           Specialty landing
/doctors                       Doctor directory
/doctors/[slug]                Doctor profile
/hospitals                     Hospital/clinic directory
/[city]/hospital/[slug]        Hospital page
/locations                     City directory
/locations/[city]              City page
/blog, /blog/[slug]            Health articles
/reviews                       Patient reviews
/contact                       Book an appointment
/about                         About the company
/faqs                          General FAQs
/privacy, /terms               Legal
```

### Original data model plan (Prisma — never built; live repo uses Mongoose, see above)

`Specialty`, `Treatment` (with `Faq[]`), `Doctor`, `City`, `Clinic`, `BlogPost`, `Testimonial`,
`Appointment` (booking submissions, status enum `PENDING/CONFIRMED/CANCELLED/COMPLETED`).

### Plan document

A designed Artifact ("Care Platform Blueprint") was published earlier summarizing the plan above
visually. It documents the **superseded** Next.js/Postgres plan, not the live TanStack/MongoDB
codebase — treat this CLAUDE.md as the current source of truth instead.
